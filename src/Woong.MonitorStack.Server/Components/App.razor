<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <title>Woong Monitor Stack Dashboard</title>
    <style>
        :root {
            color-scheme: light;
            --background: #f6f8fb;
            --surface: #ffffff;
            --border: #dde3ea;
            --primary: #0f6bde;
            --success: #16a34a;
            --warning: #f59e0b;
            --text: #0f172a;
            --muted: #64748b;
        }

        body {
            margin: 0;
            background: var(--background);
            color: var(--text);
            font-family: "Segoe UI", Arial, sans-serif;
        }

        a {
            color: var(--primary);
        }
    </style>
</head>
<body>
    <Routes />
</body>
</html>
