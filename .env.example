# Local development only. Do not reuse these values for production.
POSTGRES_DB=woong_monitor
POSTGRES_USER=woong
POSTGRES_PASSWORD=woong_dev_password
WOONG_POSTGRES_HOST_PORT=55432

# Useful when running the server from the same shell.
ConnectionStrings__MonitorDb=Host=localhost;Port=55432;Database=woong_monitor;Username=woong;Password=woong_dev_password
